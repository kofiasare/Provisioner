# {{ ansible_managed }}

# Minio local/remote volumes.
{% if minio_server_cluster_nodes | length > 0 %}
MINIO_VOLUMES="{{ minio_server_cluster_nodes | join(' ') }}"
{% else %}
MINIO_VOLUMES="{{ minio_server_datadirs | join(' ') }}"
{% endif %}
# Minio cli options.
MINIO_OPTS="--address {{ minio_server_addr }} --console-address {{ minio_console_server_aadr}} {{ minio_server_opts }}"

{% if minio_access_key %}
# Access Key of the server.
MINIO_ROOT_USER="{{ minio_root_user }}"
{% endif %}
{% if minio_secret_key %}
# Secret key of the server.
MINIO_ROOT_PASSWORD="{{ minio_root_password }}"
{% endif %}

{{ minio_server_env_extra }}
