[Unit]
Description = MailCatcher
After=network.target
After=systemd-user-sessions.service

[Service]
Type=simple
Restart=on-failure
User=vagrant
ExecStart=/home/vagrant/.rbenv/shims/mailcatcher --foreground --smtp-ip 0.0.0.0 --ip 0.0.0.0

[Install]
WantedBy=multi-user.target
